export type SpellType = {
  cantrip: {
    max: number;
  };
  level1: {
    remaining: number;
    max: number;
  };
  level2: {
    remaining: number;
    max: number;
  };
  level3: {
    remaining: number;
    max: number;
  };
  level4: {
    remaining: number;
    max: number;
  };
  level5: {
    remaining: number;
    max: number;
  };
  level6: {
    remaining: number;
    max: number;
  };
  level7: {
    remaining: number;
    max: number;
  };
  level8: {
    remaining: number;
    max: number;
  };
  level9: {
    remaining: number;
    max: number;
  };
  pact: {
    remaining: number;
    max: number;
  };
};
